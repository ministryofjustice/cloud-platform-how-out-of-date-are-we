apiVersion: v1
kind: Service
metadata:
  name: cloud-platform-reports-service
  labels:
    app: cloud-platform-reports-service
spec:
  ports:
  - port: 4567
    name: https
    targetPort: 4567
  selector:
    app: cloud-platform-reports-app
