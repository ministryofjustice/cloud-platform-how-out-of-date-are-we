apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-platform-reports.fullname" . }}
  labels:
    {{- include "cloud-platform-reports.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-platform-reports.selectorLabels" . | nindent 4 }}
